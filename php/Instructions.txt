Objects in the webroot directory are to be placed in the webroot of the server. Everything else is to be placed at the level above, so it is not accessible by the web server for security reasons. The PDF directory must have read and write access by the web server. The other directories require read access.